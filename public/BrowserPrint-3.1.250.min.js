// Zebra Browser Print SDK - Placeholder
// Download the actual SDK from: https://www.zebra.com/us/en/support-downloads/software/printer-software/browser-print.html
// This is a simplified mock for development purposes

window.BrowserPrint = {
  getDefaultDevice: function(deviceType, successCallback, errorCallback) {
    // Mock device for development
    setTimeout(() => {
      successCallback({
        name: "Zebra ZD421",
        uid: "mock-printer-1",
        connection: "network",
        deviceType: deviceType,
        version: 1,
        provider: "mock",
        manufacturer: "Zebra Technologies"
      });
    }, 100);
  },

  getLocalDevices: function(successCallback, errorCallback, deviceType) {
    setTimeout(() => {
      successCallback([
        {
          name: "Zebra ZD421",
          uid: "mock-printer-1",
          connection: "network",
          deviceType: deviceType || "printer",
          version: 1,
          provider: "mock",
          manufacturer: "Zebra Technologies"
        }
      ]);
    }, 100);
  }
};

class MockDevice {
  constructor(device) {
    this.device = device;
    this.name = device.name;
  }

  send(data, successCallback, errorCallback) {
    console.log("Sending ZPL to printer:", data);
    setTimeout(() => {
      successCallback();
    }, 500);
  }

  sendThenRead(data, successCallback, errorCallback) {
    this.send(data, successCallback, errorCallback);
  }

  read(successCallback, errorCallback) {
    setTimeout(() => {
      successCallback("");
    }, 100);
  }
}

window.BrowserPrint.Device = MockDevice;
